# Vendored AstraStoreXion client

This directory pins the small backend SDK used by the blog so a clean
`pip install -r requirements.txt` does not depend on an unpublished package.
Keep it synchronized with `AstraStoreXion/client/python` before release.
